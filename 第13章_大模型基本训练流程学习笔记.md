# 第 13 章：大模型基本训练流程学习笔记

> 依据 Datawhale 的[《Diy-LLM》第 13 章：大模型的基本训练流程](https://datawhalechina.github.io/diy-llm/chapter13/chapter13_%E7%AC%AC%E5%8D%81%E4%B8%89%E7%AB%A0%E5%A4%A7%E6%A8%A1%E5%9E%8B%E7%9A%84%E5%9F%BA%E6%9C%AC%E8%AE%AD%E7%BB%83%E6%B5%81%E7%A8%8B.html)整理。

这一章介绍大模型从“会续写”到“会按照人类要求回答”的过程。主线可以先记成：

```text
预训练 → 监督微调（SFT）→ 偏好对齐（RLHF / DPO 等）
```

不同模型的实际流程可能更复杂，训练阶段也可能互相混合，但三个阶段的目标仍然不同。

---

## 13.1 机器学习中常见的学习方式

### 13.1.1 监督学习

监督学习的数据中有输入和标准答案：

```text
输入 x → 模型预测 ŷ → 与答案 y 比较 → 计算损失
```

分类、回归和后面的 SFT 都属于监督学习。模型通过预测和答案之间的误差更新参数。

### 13.1.2 无监督与自监督学习

无监督学习没有人工提供的标签。大模型预训练更准确地说属于自监督学习：文本本身就能生成标签。

例如把一句话向右错开一位，前面的 token 是输入，后面的 token 就是答案，不需要人工逐句标注。

### 13.1.3 强化学习

强化学习没有固定的逐步答案，而是根据行为得到奖励，再提高高奖励行为出现的概率。

在大模型中：

- 状态可以看成当前提示和已经生成的 token；
- 动作是选择下一个 token；
- 策略是语言模型给出的概率分布；
- 奖励表示整段回答有多符合目标。

---

## 13.2 第一阶段：预训练

### 13.2.1 Next-token Prediction

大多数生成式语言模型使用“预测下一个 token”进行预训练。

```text
输入：自然 语言 处理
标签：语言 处理 是
```

实际上会对序列中的多个位置同时计算交叉熵损失。不断在大规模文本上做这件事后，模型逐渐学到语言规律、事实知识、代码模式和一定的推理能力。

预训练结束后得到的基础模型，本质上仍然更像一个续写模型。它可能知道很多内容，却不一定知道用户希望它以问答形式回应。

### 13.2.2 预训练数据

预训练语料可能来自网页、书籍、论文、代码和多语言文本。数据需要经过许可检查、清洗、去重、质量过滤和配比控制。

数据规模很重要，但并不是把所有抓到的文本直接混在一起。低质量、重复和有害数据同样会被模型学进去。

**Tip:** 课程中不同模型的 token 数属于特定公开口径，不能直接当成所有同规模模型的固定配方。参数量、训练 token 数、数据质量和计算预算要一起考虑。

### 13.2.3 GPT-3 的意义

GPT-3 展示了大规模自回归预训练后的 few-shot 和 in-context learning 能力：只在提示中给任务说明或少量例子，不更新参数，模型也能完成翻译、问答等任务。

但早期基础模型仍然不够听话，提示往往要写成“让模型继续补全”的形式。这也说明预训练能力和产品中的指令遵循不是一回事。

---

## 13.3 第二阶段：监督微调（SFT）

SFT 使用“问题—理想回答”数据继续训练预训练模型，让它学会按照指令回答、保持特定风格、输出结构化内容或调用工具。

可以这样区分：

```text
预训练：学习语言、知识和通用能力
SFT：学习如何把已有能力表现成目标行为
```

### 13.3.1 SFT 的作用

SFT 常用的还是 token 级交叉熵。训练时通常重点计算 assistant 回答部分的损失，让模型模仿参考答案。

SFT 可以教会模型：

- 按问答格式回应；
- 遵守输出格式；
- 进行多轮对话；
- 使用工具调用结构；
- 对明显有害的要求做适当拒绝。

### 13.3.2 SFT 数据格式

单轮指令常使用类似 Alpaca 的格式：

```json
{
  "instruction": "翻译成英文",
  "input": "你好",
  "output": "Hello"
}
```

多轮对话常使用 messages 格式：

```json
{
  "messages": [
    {"role": "system", "content": "你是客服助手"},
    {"role": "user", "content": "怎么修改收货地址？"},
    {"role": "assistant", "content": "请打开订单详情页……"}
  ]
}
```

训练前还要通过模型对应的 chat template，把角色和内容拼成真正的 token 序列。

**Tip:** Alpaca、ChatML 和 ShareGPT 不是可以随意混用的同一种文件。字段名只是外层格式，最终必须与目标模型的 tokenizer、角色标记和 loss mask 对齐。

### 13.3.3 数据质量

SFT 数据量远小于预训练数据，却能明显改变模型行为，所以质量比单纯数量更重要。

检查重点包括：

- 答案是否事实正确；
- 指令和回答是否匹配；
- 是否有重复或互相矛盾的样本；
- 风格是否过于单一；
- 代码、数学答案是否经过执行或验证；
- 训练集与验证集是否意外重叠。

合成数据可以快速扩大规模，但强模型生成的回答也可能包含幻觉和固定风格，需要过滤和抽样复核。

### 13.3.4 幻觉与灾难性遗忘

如果 SFT 数据要求模型回答它原本并不知道的事实，模型可能只学到“遇到这类问题就写出像真的答案”，从而增加幻觉。对于无法可靠回答的问题，训练数据中保留“不知道”或要求使用检索工具，可能比强迫模型给出完整答案更合理。

只用狭窄领域数据连续微调，还可能让模型损失原有通用能力，这叫灾难性遗忘。可以通过混入一部分通用数据、减小学习率、缩短训练步数以及持续做通用能力评估来观察和缓解。

个人扩展：SFT 更像“示范正确行为”，不适合单独承担所有知识注入任务。如果目标是让模型回答不断变化的资料，RAG 或工具检索通常比把每条事实都写进参数更容易更新和核查。

### 13.3.5 SFT 数据的演化

早期数据常把分类、翻译等 NLP 数据集改写成指令。后来逐渐出现：

- Self-Instruct：用少量种子指令让模型生成更多任务；
- 强模型蒸馏：用真实用户问题配合强模型回答；
- 复杂指令合成与自动过滤；
- 包含 `tool_calls`、代码执行结果和 Agent 轨迹的数据。

SFT 已经不只是模仿聊天文本，也可以看成对模型进行“行为示范”。

---

## 13.4 第三阶段：对齐人类偏好

SFT 给模型一个理想答案，但很多开放问题没有唯一答案。偏好数据会针对同一个 prompt 比较多个回答：

```text
prompt + chosen（较好回答）+ rejected（较差回答）
```

比较两个答案通常比从零写出完美答案便宜，但偏好标注也会受到标注员背景、专业水平和个人习惯影响。

### 13.4.1 偏好数据的风险

- 非专家可能更喜欢格式漂亮但事实错误的答案；
- 人和模型裁判都可能偏爱更长的回答；
- AI 标注可能偏爱与自己相似的写作风格；
- 标注模型的幻觉可能被继续放大；
- 不同群体对“有帮助、安全、合适”的理解并不完全相同。

因此，偏好分数上升不一定表示模型更聪明，也可能只是更会迎合裁判。

### 13.4.2 RLHF 的三个阶段

经典 RLHF 流程是：

```text
1. SFT：得到会遵循指令的初始模型
2. Reward Model：根据偏好对训练打分模型
3. PPO：让策略模型提高高奖励回答的概率
```

奖励模型通常接收 prompt 和回答，输出一个标量分数。训练目标是让 chosen 的分数高于 rejected。

---

## 13.5 PPO 的基本思路

PPO 要让模型朝高奖励方向更新，又不能一次改得太大。否则模型可能为了骗过奖励模型而产生奇怪输出，这叫 reward hacking。

PPO 中常见的模型角色有：

| 角色 | 作用 |
|---|---|
| 策略模型 | 当前要训练的模型，负责生成回答 |
| 参考模型 | 冻结的 SFT 模型，用于限制策略偏移 |
| 奖励模型 | 冻结的打分器，评价整段回答 |
| 价值模型 | 估计当前状态的预期回报，辅助计算优势 |

**Tip:** 经典 PPO-RLHF 中，参考模型和奖励模型通常冻结；价值模型（critic/value head）通常需要训练。不能把四个模型都理解成冻结状态。

### 13.5.1 采样与优势

当前策略先对一批 prompt 生成回答，奖励模型给出分数。优势函数表示某个动作比当前平均水平好多少：

- 优势为正，提高对应 token 的概率；
- 优势为负，降低对应 token 的概率。

### 13.5.2 重要性采样与裁剪

策略更新后，旧数据与新策略不再完全匹配。重要性权重用新旧策略对同一 token 的概率比进行修正，使一批采样结果可以使用多次。

PPO 会把概率比限制在一个较小范围内。好的动作不能一次被提高太多，差的动作也不能一次被压得太低，从而减少训练崩溃。

### 13.5.3 KL 约束

PPO 通常加入策略模型与参考模型之间的 KL 惩罚：

```text
最终奖励 = 奖励模型分数 - KL 惩罚
```

这样可以防止模型为了追求高奖励而离原来的语言能力太远。

PPO 的缺点是流程复杂，需要多次生成、多个模型前向和较大的显存，调参也比较困难。

---

## 13.6 DPO（Direct Preference Optimization）

DPO 直接使用 chosen/rejected 偏好对训练，不再先训练奖励模型，也不需要完整的在线强化学习循环。

它的直观目标是：

```text
提高 chosen 的相对概率
降低 rejected 的相对概率
同时参考原 SFT 模型，避免偏离过远
```

### 13.6.1 DPO 训练流程

```text
准备 prompt、chosen、rejected
→ 分别计算策略模型的序列对数概率
→ 计算参考模型的序列对数概率
→ 计算 DPO loss
→ 反向传播更新策略模型
```

DPO 比 PPO 简单很多，也更像普通监督训练，但它仍然依赖高质量偏好对。如果 chosen 只是更长、格式更漂亮，模型也会学到这种偏差。

### 13.6.2 DPO 的变体

- SimPO：不再依赖参考模型，并加入 margin；
- 长度归一化：比较平均每个 token 的概率，减少长回答天然占优的问题。

**Tip:** DPO 不是“任何 SFT 数据换个损失就能用”。它需要同一 prompt 下有明确的较好和较差回答，而且偏好标准要稳定。

个人扩展：对于个人或小团队项目，更现实的路线通常是“选择合适的开源基础模型 → 用少量高质量数据做 LoRA/SFT → 建立任务评估 → 确有需要再做 DPO”。从零预训练或完整 PPO-RLHF 对数据、显存和工程要求都高得多。

---

## 个人扩展：三个阶段放在一起看

| 阶段 | 主要数据 | 主要目标 | 得到的变化 |
|---|---|---|---|
| 预训练 | 大规模原始文本 | 预测下一个 token | 获得语言、知识和通用能力 |
| SFT | 指令与理想回答 | 模仿示范答案 | 学会问答格式、工具调用和目标行为 |
| 偏好对齐 | chosen/rejected 或奖励 | 更符合偏好与安全要求 | 调整回答取舍和风格 |

这三步不是简单地“每做一步模型就一定更强”。后训练主要改变模型如何使用和展示已有能力，如果数据或奖励设计不合理，也可能造成幻觉、过度拒答、回答变长或通用能力下降。
